---
description: Project memory — paths, env, and conventions for Gapta
alwaysApply: true
---

# Project Memory (Gapta)

Single source of truth for paths, env vars, and tools. Run commands from the **project root** (`D:\QA_A\Gapta` or repo root) unless stated otherwise.

## Environment and API keys

- **File:** `.env` in the **project root** (gitignored).
- **Variables:**
  - `GEMINI_API_KEY` — nano-banana-pro (image generation)
  - `GITHUB_PERSONAL_ACCESS_TOKEN` — GitHub MCP
  - `EXA_API_KEY` — Exa MCP
  - `CONTEXT7_API_KEY` — Context7 MCP (see [.cursor/README-MCP.md](.cursor/README-MCP.md))
- Never commit secrets. Prefer env / `.env`; committed [.cursor/mcp.json](.cursor/mcp.json) has **no** embedded tokens — keys must be in the environment.

## Nano Banana Pro

- **Skill:** `.cursor/skills/nano-banana-pro/`
- **Run from project root** with `uv run` and `GEMINI_API_KEY` loaded.

## MCP

- **Config:** `.cursor/mcp.json` + [.cursor/mcp.example.json](.cursor/mcp.example.json)
- Details: [.cursor/README-MCP.md](.cursor/README-MCP.md)

## Articles and content (when blog exists in repo)

- **Path:** `src/content/articles/{slug}/index.mdx`, `imgs/`, optional `components/`
- **Rules:** `article-writing.mdc`, `content-pipeline.mdc`
- **Language:** Ukrainian for user-facing copy.
- **Topic:** computerized embroidery (Gapta / gapta.com.ua).
- **Git:** no direct commits to **`main`**; feature branches; `conventional-commits` skill.

## Git flow for new articles (Stage 5)

- **Branch from `main`:** `git checkout main && git pull origin main && git checkout -b article/{slug}`
- **Commit:** e.g. `feat(article): add …` or `content(article): update …`
- **Push:** `git push -u origin article/{slug}`
- **PR:** GitHub MCP `create_pull_request`: owner `AZANIR`, repo **`Gapta`**, base **`main`**, head = branch name. Create the PR after push for article/content work.
- **Publish:** merge to **`main`** → [.github/workflows/deploy.yml](.github/workflows/deploy.yml). Full guide: [.cursor/docs/FLOW.md](.cursor/docs/FLOW.md).

## Project notes (optional)

If `docs/project_notes/` exists, keep bugs/decisions/key facts there and update after tasks ([task-completion.mdc](task-completion.mdc)). If the folder is missing, skip or create when the team adopts that workflow.

## Summary

- Keys in root `.env` / environment; not in committed MCP JSON.
- **Repo:** AZANIR/**Gapta**, default branch **`main`**, deploy **npm** + rsync (see AGENTS.md).
- **Article skill:** `gapta-article-writer`.
